jobProperties:
    container_suffix: '-spark:'
    second_repo: git@github.com:darbotdb/darbotdb-spark-datasource.git
    branch: main
    init_command: ''
tests:
- spark_datasource:
    job: run-driver-tests
    options:
      priority: 500
      type: single
    args:
      extremeVerbosity: true
- spark_datasource:
    job: run-driver-tests
    options:
      priority: 500
      type: cluster
    args:
      extremeVerbosity: true
